import express from "express";

const authorRoutes = express.Router();


authorRoutes.get();
authorRoutes.get();
authorRoutes.put();
authorRoutes.post();
authorRoutes.delete();

export default authorRoutes;